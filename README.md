# wow-tracekit
TraceKit is a lightweight tracing library for WoW addon developers. Create a tagged tracer with `TraceKit('&lt;Addon Name>')` and call it like a function to send labeled debug messages to Blizzard's Event Trace UI.
